[package]
name = "zellij-agent-wrangler"
version = "0.1.0"
edition = "2021"
description = "A zellij sidebar listing tabs, panes and agent sessions (prototype)"

[lib]
name = "zellij_agent_wrangler"
path = "src/lib.rs"

# The plugin itself calls zellij's host functions, which exist only inside the
# wasm host, so it is built for that target and tested through the library.
[[bin]]
name = "zellij-agent-wrangler"
path = "src/main.rs"
required-features = ["plugin"]
test = false

# The hook client runs as an ordinary process beside the agent that invokes it.
# Its feature is what keeps it, and the JSON it reads, out of the wasm.
[[bin]]
name = "zellij-wrangler"
path = "src/wrangler.rs"
required-features = ["native"]

# Two halves that share a wire format and nothing else. Both are on by default
# so `cargo test` covers the whole crate; each build turns off the half it does
# not want. Off wasm, `zellij-tile` brings in the whole of `zellij-utils` -
# curl, openssl, rusqlite - which the hook client has no use for.
[features]
default = ["native", "plugin"]
native = ["dep:serde_json"]
plugin = ["dep:zellij-tile"]

[dependencies]
zellij-tile = { version = "0.44.3", optional = true }
serde_json = { version = "1", optional = true, features = ["preserve_order"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
